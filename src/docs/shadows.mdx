import { Meta, Typeset } from '@storybook/blocks';
import { ShadowsTable } from '@/../.storybook/utils';

import { shadows } from '@/tokens/shadows';

<Meta title="Design System/Tokens/Shadows" />

# Shadows

The use of elevation and drop shadows offers a way to add depth and dimension to interface. This additional layer of complexity can make elements pop, signify interactivity, and guide users in their journey through the app.

<ShadowsTable of={shadows} />
