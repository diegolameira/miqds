import { Meta, Typeset } from '@storybook/blocks';
import { RadiiTable } from '@/../.storybook/utils';

import { radii } from '@/tokens/radius';

<Meta title="Design System/Tokens/Radius" />

# Radius

The radius is used to control the roundness of the corners of our UI elements. It helps to maintain consistency and visual harmony across different components and layouts.

<RadiiTable of={radii} />
