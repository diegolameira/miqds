import { Meta, Typeset } from '@storybook/blocks';
import { spacing } from '$tokens/spacing';
import { SpacingTable } from '../../.storybook/utils';

<Meta title="Design System/Tokens/Spacing" />

# Spacing

Spacing is not merely an afterthought; it's an integral part of the design, shaping how elements relate to each other and to the overall layout. Good spacing can improve readability, create focus, and optimize the user experience.

<SpacingTable of={spacing} />
